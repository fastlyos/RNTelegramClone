import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import PropTypes from 'prop-types';

export default function Badge({ number, status, containerStyle }) {
  return (
    <View>
      <Text></Text>
    </View>
  );
}

Badge.propTypes = {};

const styles = StyleSheet.create({});
